# Delivery log — see ../schema.cue #Delivery and ../README.md ## Delivery Log.
# Validate: cue vet -d '#Delivery' ../schema.cue delivery.yaml
log:
  - date: "2026-08-25"
    summary: >-
      Render-parity harness: kernel vs pure-CUE oracle over shipped catalog and probe transformers; four divergences recorded (library PR 68)
    change: { kind: openspec, repo: library, change: library-parity-harness }
    decisions: [D1, D14]
  - date: "2026-08-25"
    summary: >-
      Fill #component from the evaluated components value; FinalizeValue off the render path; names-probe and env-hoisting divergences retired; D14 migration note (library PR 70)
    change: { kind: openspec, repo: library, change: library-component-fill }
    decisions: [D1, D2, D3]
  - date: "2026-08-25"
    summary: >-
      Fill #moduleInstance with the whole evaluated instance; instance-probe divergence retired; closes library#65 (library PR 71)
    change: { kind: openspec, repo: library, change: library-instance-fill }
    decisions: [D1, D3]
  - date: "2026-08-25"
    summary: >-
      Remove compile.FinalizeValue, Kernel.Finalize and Execute's dataComponents; security-audit skill states the fill-as-evaluated boundary (library PR 72)
    change: { kind: openspec, repo: library, change: library-finalize-removal }
    decisions: [D1]
  - date: "2026-08-25"
    summary: >-
      resourceName defaults to <instance>-<component>; D16 mechanism refined (unvalidated default branch, guarded hidden length assertion, error() diagnostics with the remedy); new component-names spec; closes core#49 (core PR 51)
    change: { kind: openspec, repo: core, change: core-resourcename-default }
    decisions: [D16]
  - date: "2026-08-26"
    summary: >-
      Three name types, per-primitive #nameConstraint slot, hidden resolved-name assertion on #Component; resourceName ceiling to #ObjectNameType, D16 length guard retired; #ComponentNames widened (core PR 52)
    change: { kind: openspec, repo: core, change: core-name-types-and-constraint }
    decisions: [D20, D21, D23]
  - date: "2026-08-27"
    summary: >-
      catalog_opm: Expose/stateful/namespaces declare nameConstraint, expose.name required and defaulted from names.dns.short, container constraint computed on the wrapper (D24) (catalog_opm PR 51)
    change: { kind: openspec, repo: catalog, change: catalog-name-constraints }
    decisions: [D21, D22, D23, D24]
  - date: "2026-08-27"
    summary: >-
      catalogs/k8s: CSIDriver (exact-name) and VolumeSnapshotClass (override-honouring) members and transformers (catalog_opm PR 57)
    change: { kind: openspec, repo: catalog, change: catalog-k8s-csi-resources }
    decisions: [D19]
  - date: "2026-08-27"
    summary: >-
      catalogs/k8s: all 25 uniform transformers read #component.#names.resourceName; apiservice and the objects user segment exact; first k8s/ fixtures (catalog_opm PR 58)
    change: { kind: openspec, repo: catalog, change: catalog-names-readonly-k8s }
    decisions: [D15, D19]
  - date: "2026-08-28"
    summary: >-
      catalogs/opm: route and network policy transformers read #component.#names.resourceName; routes require Expose and reference the Service via expose.name; read rule and label-copy rule landed in docs (catalog_opm PR 61)
    change: { kind: openspec, repo: catalog, change: catalog-names-readonly-opm-rest }
    decisions: [D15]
  - date: "2026-08-28"
    summary: >-
      catalogs/opm: the seven workload transformers read #component.#names.resourceName through the #WorkloadName seam; #ResourceNameTrait deprecated, deletion deferred to a later change (catalog_opm PR 64)
    change: { kind: openspec, repo: catalog, change: catalog-names-readonly-workloads }
    decisions: [D15]
  - date: "2026-08-28"
    summary: >-
      istio_ambient migrated from #ResourceNameTrait to metadata.resourceName with guards on #names.resourceName; fleet pinned to catalogs/opm 2.0.0-alpha.7 (sweep 2 of the trait retirement)
    change: { kind: openspec, repo: modules, change: istio-ambient-resource-name }
    decisions: [D15]
  - date: "2026-08-28"
    summary: >-
      catalog_opm: #ServiceName fallback so components built against opm alpha.5 and earlier (optional, unset expose.name) render their Service, StatefulSet serviceName and route backendRefs on alpha.8+
    change: { kind: openspec, repo: catalog, change: service-transformer-legacy-expose-fallback }
    decisions: [D22]
  - date: "2026-08-30"
    summary: >-
      Shared-platform concurrency promise retracted: ADR-002 marked superseded in place, kernel docs and specs state the two supported shapes, -race added to task test, MIGRATIONS.md entry (library PR 93)
    change: { kind: pr, repo: library, number: 93 }
  - date: "2026-08-30"
    summary: >-
      Interim stopgap: every shared Kernel use serialised behind Store.AcquireKernel across the three controllers; renders serialised process-wide until the shares-nothing render lands (opm-operator PR 110)
    change: { kind: pr, repo: opm-operator, number: 110 }
  - date: "2026-08-31"
    summary: >-
      catalog_opm: #ResourceNameTrait, #ResourceName wrapper, #ResourceNameSchema and the #WorkloadName seam deleted; seven workload transformers read #component.#names.resourceName directly; catalogs/opm crosses to major v4 (4.0.0)
    change: { kind: openspec, repo: catalog, change: catalog-drop-resource-name-trait }
    decisions: [D15, D25]
  - date: "2026-09-01"
    summary: >-
      Fleet re-pinned to catalogs/opm@v4.0.0; k8up updated for removed EnvVarSchema.from
    change: { kind: openspec, repo: modules, change: fleet-repin-catalog-v4 }
    decisions: [D15, D16]
  - date: "2026-09-01"
    summary: >-
      core-context-projection: #transform.#context projected from the other two inputs (0019 D12) landed in core
    change: { kind: openspec, repo: core, change: core-context-projection }
    decisions: [D12]
  - date: "2026-09-01"
    summary: >-
      core: registry entries carry the catalog by import; #Subscription and #matchers removed
    change: { kind: openspec, repo: core, change: core-registry-import }
    decisions: [D5, D17]
  - date: "2026-09-02"
    summary: >-
      library: module.Source broadened (Pkg, on-disk mode); Instance.Source and Platform.Source; synth.Instance returns the staged tree; Kernel.AcquirePlatformFromDir and AcquireInstanceFromDir (0019 D9 input shape)
    change: { kind: openspec, repo: library, change: library-platform-source }
    decisions: [D9]
  - date: "2026-09-03"
    summary: >-
      library: Kernel.Render single-build render path (staging, D13 promotion + coverage invariant, D7/D18 skew policy, D10 in-build matching with verdicts as data, per-render context), additive beside Compile
    change: { kind: openspec, repo: library, change: library-render-build }
    decisions: [D7, D9, D10, D13, D18]
  - date: "2026-09-04"
    summary: >-
      Library render cutover: Kernel.Render is the sole render path; phase methods, platform synthesis/materialization/matching packages retired
    change: { kind: openspec, repo: library, change: library-render-cutover }
    decisions: [D8, D9, D10, D13, D26]
  - date: "2026-09-04"
    summary: >-
      opm-operator: Platform reconciler generates and builds a build-local platform CUE module from the CR (full-closure pins, per-generation swap); Generated/BuildFailed/GenerateFailed reasons
    change: { kind: openspec, repo: opm-operator, change: operator-platform-module-generation }
    decisions: [D6]
  - date: "2026-09-04"
    summary: >-
      opm-operator: render switch to the single-build Kernel.Render (leased platform record, gate held only across acquisition); spec.skewPolicy Warn|Refuse, SkewRefused reason, RenderWarning events; --max-concurrent-renders
    change: { kind: openspec, repo: opm-operator, change: operator-render-switch }
    decisions: [D7, D8, D18]
  - date: "2026-09-04"
    summary: >-
      cli: opm config init seeds ~/.opm/platform/ as a CUE module (pins in cue.mod, entries import their catalogs); config vet builds it through the kernel loader; legacy platform.cue migrated
    change: { kind: openspec, repo: cli, change: cli-config-platform-module }
    decisions: [D5]
no_work:
  D4: >-
    The sequencing decision itself (the Phase A/B split): implemented by the
    order the work lands in, visible in this log as it accretes, rather than
    by any single change.
  D11: >-
    An authoring contract, never structurally enforced: sibling access
    through #moduleInstance stays reachable and is discouraged by catalog
    review; the first-party transformer sweep keeps the catalog exemplary,
    and no code change carries it.
