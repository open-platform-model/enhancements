# Delivery log — see ../schema.cue #Delivery and ../README.md ## Delivery Log.
# Validate: cue vet -d '#Delivery' ../schema.cue delivery.yaml
log: []
no_work:
  D4: >-
    The sequencing decision itself (the Phase A/B split): implemented by the
    order the work lands in, visible in this log as it accretes, rather than
    by any single change.
  D11: >-
    An authoring contract, never structurally enforced: sibling access
    through #moduleInstance stays reachable and is discouraged by catalog
    review; the first-party transformer sweep keeps the catalog exemplary,
    and no code change carries it.
