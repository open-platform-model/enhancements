# Built harness binary and the materialized scratch tree. The results files
# under _out/ ARE committed; the tree that produced them is not (it is tens of
# thousands of generated files and is rebuilt by run.sh).
/module-scale-cost
/_out/run/
