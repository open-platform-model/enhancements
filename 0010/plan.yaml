# Structured slice plan for this enhancement. Optional — see
# 0000/README.md ## Slice Plan for the field reference and the
# workflow this feeds (task vet, task plan:graph, task plan:ready,
# task slice:seed). Delete this file if the enhancement never
# grows past one slice.
#
# Structural backing for 06-operational.md ## Cross-Repo Coordination,
# which keeps the narrative rationale. That order is strict because each
# step consumes a published artifact from the one before.
#
# ---------------------------------------------------------------------
# THE TWO PHASES ARE A FIELD, NOT A CONVENTION
# ---------------------------------------------------------------------
# Every slice carries `phase: implementation | migration` (enhancements/
# schema.cue #SlicePhase), and `task vet` enforces the ordering as an edge
# rule: no implementation slice may depend on a migration slice. So the
# split is checked rather than described, and this block no longer
# enumerates which slices fall where — read the field, or `task plan:graph`.
#
# What the field cannot record is WHY the seam falls where it does. The
# boundary here is the first `opm catalog publish` of a conforming
# catalog; implementation is dependency-closed behind it.
#
# NO SLICE STRADDLES THE BOUNDARY. That is why catalogs-identity-authoring
# and modules-identity-authoring exist as separate slices from the two
# republishes: authoring the CUE is a source change and reviewable as one;
# pushing the bytes is irreversible under 0011 D10. An earlier revision of
# this plan bundled each pair into one slice, which hid the seam and made
# the split unstateable.
#
# CLUSTERS AND ENVIRONMENTS ARE OUT OF SCOPE (2026-08-05, author's call).
# These entries change what a published artifact IS; moving a running
# deployment onto it is separate work, handled afterwards and privately.
# Named here so it is not rediscovered as a gap:
#   - the live-instance relabel pass and its positive check
#     (04-graduation.md accepted->implemented gate)
#   - the workload-type cutover on already-deployed objects
#     (04-graduation.md gate; see 05-risks.md for why it is a recreate)
#   - rewriting a live Platform's subscriptions from `range` to the D14
#     scalar `version`
#   - 0011 D17's registry cleanup; its item 1 (legacy v1alpha1) stays
#     gated on the v0 -> v1 fleet migration
#   - opm-releases' own v0 -> v1 migration (see releases-repin below)
#   - the Zot registry cutover, which gates 0011 D10 (tag immutability)
#     and D11 (authenticated writes) and has no owner or date
#
# ---------------------------------------------------------------------
# NO CORE MAJOR BUMP
# ---------------------------------------------------------------------
# 06-operational.md ## Semver Impact and ## Cross-Repo Coordination step 1
# have been reconciled, but the reason is worth carrying here too:
# `opmodel.dev/core@v1` has published only v1.0.0-alpha.1/.2/.3 and no
# stable v1.0.0, so the compatibility promise has not started and the
# break lands in v1.0.0-alpha.4 with the module path unmoved. Downstream
# retarget is an ordinary dep bump (`task update-deps`); rollback is a
# re-pin to alpha.3.
#
# THE CORE SLICES ARE ONE RELEASE, NOT THREE. core-identity-shape,
# core-primitive-keying and core-platform-and-match land into the same
# unpublished alpha line; core-alpha-release is the single cut point
# 06-operational.md's "nothing else can move until this tag exists"
# describes, and it is what every downstream slice depends on.
#
# ---------------------------------------------------------------------
# CROSS-ENHANCEMENT EDGES TO 0011
# ---------------------------------------------------------------------
# These are ORDERING edges, not blocking ones — neither entry blocks the
# other at the entry level (0011 D17 dissolved the joint window). Four:
#   core-alpha-release          -> 0011:core-identity-package
#     #IdentityPackage ships in `core` (0011 D21) and belongs in the same
#     alpha the catalogs and the fleet retarget to.
#   library-subscription-collapse -> 0011:library-compat-comparator
#     D14 DELETES materialize/filter.go and 0011's predecessor selection
#     reuses filter.go:112's highestStable. A move, so it moves first.
#   catalogs-republish          -> 0011:catalogs-publish-cutover
#   modules-fleet-republish     -> 0011:modules-publish-cutover
#     The republishes go through `opm catalog publish` / `opm module
#     publish`, so the CI cutover must already be in place. The mirror
#     edges are declared in 0011's plan: both cutover slices point a
#     release pipeline at a command that REFUSES a tree with no conformant
#     identity package (0011 D21 refusal 10, D4), so each depends on the
#     authoring slice here that creates those files.
slices:
  - id: core-identity-shape
    repo: core
    phase: implementation
    concern: >
      Module, instance and catalog identity: #ModulePathType gains @vN and
      underscores, #PackagePathType splits off, fqn == modulePath,
      registryPath, snake name, instance fqn/uuid (D1, D6, D8, D38, D40,
      D41, D43).
    depends_on: []
    status: planned

  - id: core-primitive-keying
    repo: core
    phase: implementation
    concern: >
      Contract keys split from implementation keys: #ContractFQNType /
      #ImplFQNType, #APIVersionType, apiVersion! and catalogVersion! on the
      primitives, authored fqn, primitive-versus-adapter (D4, D21, D25, D33,
      D34, D44).
    depends_on:
      - core-identity-shape
    status: planned

  - id: core-platform-and-match
    repo: core
    phase: implementation
    concern: >
      Platform and match surface: #SubscriptionFilter deleted for a scalar
      version (D14), matchLabels on the four kinds with #LabelWorkloadType
      deleted (D36), fulfilment on #Resource and #Trait (D37).
    depends_on:
      - core-identity-shape
    status: planned

  - id: core-alpha-release
    repo: core
    phase: implementation
    concern: >
      The single cut point. SPEC.md coherence pass across the three schema
      slices, schemas/examples.cue re-vetted, v1.0.0-alpha.4 published. No
      major bump — the module path does not move.
    depends_on:
      - core-identity-shape
      - core-primitive-keying
      - core-platform-and-match
      - "0011:core-identity-package"
    status: planned

  - id: library-core-retarget
    repo: library
    phase: implementation
    concern: >
      Re-pin to v1.0.0-alpha.4 and fix the compile breakage. No behaviour
      change and no new check — this slice exists so the three behaviour
      slices below start from a building tree.
    depends_on:
      - core-alpha-release
    status: planned

  - id: library-identity-read-checks
    repo: library
    phase: implementation
    concern: >
      Identity verified where artifacts are read: modulePath against the
      fetched coordinate and version against the tag on the acquire path,
      plus materialize and subscription (D11, D9, D41's instance FQN).
    depends_on:
      - library-core-retarget
    status: planned

  - id: library-subscription-collapse
    repo: library
    phase: implementation
    concern: >
      D14 deletes materialize/filter.go — no highestStable default, no
      constraint solving, no prerelease inference. What survives is one
      major-agreement check beside the subscription.
    depends_on:
      - library-core-retarget
      - "0011:library-compat-comparator"
    status: planned

  - id: library-contract-match
    repo: library
    phase: implementation
    concern: >
      The match rung becomes load-bearing: provenance denylist before
      unification (D26/D30), contract-key diagnostics (D4), unresolved
      demand is an error (D28), single-provider guard at materialize
      (D32/D37).
    depends_on:
      - library-core-retarget
    status: planned

  - id: cli-coordinate-adoption
    repo: cli
    phase: implementation
    concern: >
      Retarget to the new library, delete the address-composition helpers,
      write the resolved coordinate into spec.module.{path,version}.
    depends_on:
      - library-core-retarget
    status: planned

  - id: operator-library-retarget
    repo: opm-operator
    phase: implementation
    concern: >
      Retarget to the new library. No feature change and no adoption code —
      D18 rejected an operator-side tolerance window outright.
    depends_on:
      - library-core-retarget
    status: planned

  - id: catalogs-identity-authoring
    repo: catalog
    phase: implementation
    concern: >
      Commit identity/identity.cue in the D5 shape across the three
      catalogs, re-key every leaf (apiVersion, catalogVersion, authored
      fqn), flatten the blueprints (D42), move matching to matchLabels,
      delete the stamping task. Source only.
    depends_on:
      - library-subscription-collapse
      - library-contract-match
    status: planned

  - id: modules-identity-authoring
    repo: modules
    phase: implementation
    concern: >
      Add identity/identity.cue per module and the D12 metadata derivation,
      validated against local catalog checkouts. No hyphen renames —
      measured 2026-08-05, none exist on either branch. Source only,
      nothing published.
    depends_on:
      - catalogs-identity-authoring
    status: planned

  - id: catalogs-republish
    repo: catalog
    phase: migration
    concern: >
      Cut releases of catalog_opm, catalog_kubernetes and
      catalog_opm_experimental through `opm catalog publish`. The first act
      that changes a published artifact.
    depends_on:
      - catalogs-identity-authoring
      - "0011:catalogs-publish-cutover"
    status: planned

  - id: modules-fleet-republish
    repo: modules
    phase: migration
    concern: >
      Republish the fleet through `opm module publish` against the released
      catalogs. The only fleet republish in the 0010/0011 pair (0011 D17).
    depends_on:
      - catalogs-republish
      - modules-identity-authoring
      - "0011:modules-publish-cutover"
    status: planned

  - id: releases-repin
    repo: releases
    phase: migration
    concern: >
      Re-pin opm-releases to the republished module coordinates. Sibling
      repo, not under the workspace root.
    depends_on:
      - modules-fleet-republish
    status: cancelled
    cancelled_reason: >
      Environments are out of scope (2026-08-05, author's call) —
      opm-releases holds per-environment ModuleRelease configs and is
      handled outside these entries. The slice was also never viable as
      written: measured 2026-08-05, all 31 of its cue.mod/module.cue files
      pin opmodel.dev/core/v1alpha1@v1.3.10 and opmodel.dev/opm/v1alpha1@v1.5.9
      on CUE v0.15.x, and the module coordinates they consume
      (jellyfin@v1, k8up@v1, seerr@v0, cert_manager@v0 and sixteen more)
      are the v0_legacy fleet, with ZERO overlap with the eleven v1-line
      modules modules-fleet-republish republishes. So there is nothing here
      to re-pin in either direction. What opm-releases actually needs is a
      v0 -> v1 migration — the same one 0011 D17 records as the gate on
      deleting the legacy v1alpha1 repositories — which is a separate piece
      of work rather than a step of this entry.
