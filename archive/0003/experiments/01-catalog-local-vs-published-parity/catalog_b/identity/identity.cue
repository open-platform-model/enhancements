package identity

ModulePath: "testing.opmodel.dev/exp0003/cat_b"

// Method B: the concrete version is committed. Publish DERIVES the release
// tag from it (0003 D4); nothing is stamped, so artifact bytes == source bytes.
Version: "1.0.0"
