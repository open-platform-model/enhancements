// Template core-schema-delta module — id 0000 reserved. `task new
// CORE_SCHEMA=true` copies this directory and rewrites the 0000 prefix to
// the new four-digit id. Language version tracks opmodel.dev/core. Add a
// dep on opmodel.dev/core@vN (`cue mod tidy` from schemas/) when the delta
// imports unchanged core types.
module: "enhancements.opmodel.dev/0000/schemas@v0"
language: {
	version: "v0.17.0"
}
