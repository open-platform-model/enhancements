# Template config.yaml — id 0000 is reserved for this template.
#
# When creating a new enhancement, copy this directory to enhancements/NNNN/
# (next available four-digit id) and overwrite every value below.
#
# This file is itself a valid #EnhancementConfig — running
# `cue vet -d '#EnhancementConfig' enhancements/schema.cue enhancements/0000/config.yaml`
# passes. The "placeholder-ness" lives in the slug, title, and markdown bodies.

id: "0007"
slug: manifest-passthrough
title: 'Manifest Passthrough: Side-Channel Raw and Kustomize Manifests'
status: draft
# Which OPM area is the primary owner. See enhancements/schema.cue #Area.
area: cross-cutting
# Every repo that ships code/schema/content changes driven by this enhancement.
# MUST include `area`. Validated against the workspace directory map in /CLAUDE.md.
affects:
  - cross-cutting
  - cli
  - opm-operator
core_schema: false
created: "2026-06-23"
updated: "2026-08-20"
authors:
  - emil@jacero.se
# Implementation tracking. status: not-started | in-progress | partial | complete.
# `date` and `notes` allowed only when status reaches `complete`.
implementation:
  status: not-started
# Append-only timeline of milestones. The validator (future) enforces
# append-only via git diff against HEAD~1. Free-form `event` strings;
# `slice` + `semver` are optional structured fields for events that carry
# machine-readable detail.
history:
  - date: "2026-06-23"
    event: Drafted
  - date: "2026-06-23"
    event: "Design direction set from architecture review — apply-layer passthrough on the CLI + operator (kustomize embedded via krusty, output folded into the existing SSA/inventory/prune machinery); core schema and library kernel held untouched to preserve kernel purity. Two scope questions seeded as Open Questions (kustomize-build vs raw-only; relationship to 0005)"
  - date: "2026-08-04"
    event: "Illustrative example vocabulary genericised across this entry — a documentation-hygiene pass, cosmetic only. NO decision, gate, open question, measurement or schema constraint changed, and no decision or OQ number moved. The worked examples' application names move to neutral infrastructure names (`web-app` / `opmodel.dev/modules/web_app`), and example namespaces from `media`/`games-*` to `prod`/`staging`/`data-*`. DELIBERATELY LEFT VERBATIM: dated measurements (the 2026-07-25 byte-for-byte registry read and everything derived from it), live-fleet inventories, and real repository paths — `modules/jellyfin/`, `opm-kind-demo/jellyfin/` and the `modules/*` rewire lists name artifacts that were actually read or must actually be opened, so renaming them would falsify the record or dangle the pointer. Where an entry's problem statement IS the measurement, it stays; where the name was arbitrary, it moved."
  - date: "2026-08-20"
    event: "Schemas convention migration: config gains core_schema: false — the entry changes no opmodel.dev/core definitions, so its compilable CUE moved from schemas/ to contracts/ (file renamed target.cue -> contracts.cue, package renamed to contracts, content unchanged); cue.mod refreshed to enhancements.opmodel.dev/0007/contracts, CUE v0.17.0. Structure/metadata only."
# Peer enhancements this one builds on or interacts with.
# Accepts new four-digit ids ("0042") and legacy three-digit refs
# ("legacy:003") that point at frozen library/enhancements/ predecessors.
related: ["0005", "0006", "0002"]
# Lifecycle relationships.
supersedes: []
superseded_by: null
