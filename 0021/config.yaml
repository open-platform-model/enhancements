id: "0021"
slug: versioning-policy
title: OPM Versioning Policy
# One line: the capability OPM will have and does not have today, or the
# rework of one it has. The `feature` admission gate (gates.cue) made durable;
# `task archive:list` renders rejected ideas by this field.
summary: >-
  One versioning policy that names every published OPM artifact class, defines its compatibility surface and bump rules, and states how each rule is enforced
status: draft

# Which OPM area is the primary owner. See enhancements/schema.cue #Area.
area: cross-cutting

# Every repo that ships code/schema/content changes driven by this enhancement.
# MUST include `area`. Validated against the workspace directory map in /CLAUDE.md.
affects:
  - cross-cutting
  - core
  - catalog
  - modules
  - cli
  - library
  - opm-operator
  - opmodel.dev

# Does this enhancement add or change opmodel.dev/core definitions?
# false → schemas/ MUST NOT exist; non-core compilable CUE lives in contracts/.
core_schema: false

created: "2026-08-24"
updated: "2026-08-30"

authors:
  - emil@jacero.se

# Append-only timeline of design milestones. Landings go to delivery.yaml.
history:
  - date: "2026-08-24"
    event: Drafted
  - date: "2026-08-25"
    event: "D3 reversed to verbatim copies; D4 fixes nine classes; policy/ split one file per class; OQ14..OQ16 (tooling train, docs, kernel ledger)"
  - date: "2026-08-25"
    event: "D5: alpha contracts encouraged to bump the alpha number on a break"
  - date: "2026-08-25"
    event: "D6: transformers serve a level per registration with a shared body; normalizer as backup; OQ17"
  - date: "2026-08-25"
    event: "Experiment 01-policy-as-cue concluded: policy as CUE renders Markdown with no Go, invariants refused at vet"
  - date: "2026-08-25"
    event: "Experiment 02-config-compat-gate concluded: catalog walk on #config classifies D2 table; default change is breaking"

  - date: "2026-08-30"
    event: 'ADHD readability pass: claims front-loaded, sentences split, em-dashes removed, lists capped. No answer, number, Source, Kind or Status changed. Under the 2026-08-30 readability carve-out.'
# Entries whose decisions this entry's decisions depend on. An edge exists
# iff a decision depends on a decision: list MMMM here only when a live
# `### DN:` block in 03-decisions.md carries `**Depends:** MMMM:DN`, and
# list every MMMM those lines name. Four-digit ids only (a dependency
# resolves to a decision heading; legacy:NNN has none). `task vet` enforces
# both directions and refuses a cycle.
depends_on: []

# Lifecycle relationships.
supersedes: []
revives: []
superseded_by: null
