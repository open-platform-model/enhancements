id: "0015"
slug: catalog-contracts-and-provider-registration
title: Catalog Contracts and Transformer Registration
# Aggregate design impact: schema-additive but a break in effect — the
# inventory is trivial until every catalog republishes with populated
# contract maps (06-operational.md ## Semver Impact).
semver: major
status: accepted
# Which OPM area is the primary owner. See enhancements/schema.cue #Area.
area: cross-cutting
# Every repo that ships code/schema/content changes driven by this enhancement.
affects:
  - cross-cutting
  - core
  - library
  - opm-operator
  - cli
  - catalog
core_schema: true
created: "2026-08-05"
updated: "2026-08-21"
authors:
  - emil@jacero.se
implementation:
  status: not-started
history:
  - date: "2026-08-05"
    event: >
      Drafted from a design conversation grounded in three measurements taken the same day.
      `core/src/catalog.cue` carries exactly ONE member map, `#transformers`, and
      `library/opm/materialize/index.go:39-41` skips any catalog build without one — so a
      contract with no adapter is absent from the materialized world, which is precisely the
      state 0010 D37's `fulfilment: "provider"` creates on purpose. `compile/match.go:344-360`
      is SUBSET containment, not equality, so a more-specific transformer never displaces a
      less-specific one; both pair and both render, and 0010 D32's guard does not reach the
      case because it fires only for `fulfilment: "provider"` contracts. And the real
      discriminator across the 8 transformers sharing `catalog_opm`'s `#ContainerResource`
      bucket is `requiredLabels` (`daemonset_transformer.cue:27-29`), not the resource/trait
      set — which is what makes 0010 D36's `matchLabels` the lever for provider classes rather
      than a new matcher concept. Four decisions locked: D1 (explicit contract maps), D2
      (classes, amending 0010 D37), D3 (RBAC-gated registration CR), D4 (no contract/adapter
      packaging split, decided now because riding 0010's FQN break is nearly free and a
      standalone break later is not). Six Open Questions carried.
  - date: "2026-08-19"
    event: "Enhancement 0019 rescoped to carry the single-build render pipeline as its Phase B (its D9: one CUE build per render; D5: registry entries import their catalog and embed the transformer map, version scalar removed; D6: the operator generates the platform package; D8: ADR-002 superseded, store.go's held slot removed; D10: matching moves into the build). This entry's concept layer (D1-D4: contract maps, classes, TransformerRegistration, packaging) is untouched, but its library and operator integration surface is written against code 0019 deletes: the match.go line anchors, the materialize-based inventory, and the store re-keying in OQ6 all need re-baselining, and OQ6's blast-radius question largely dissolves under 0019 D8 (no held platform to invalidate). 0019's OQ9/OQ10 (what regenerates the platform package when a registration is accepted, and where a conflicting registration's requirements are refused) are deferred TO this entry. Re-baseline is triggered when 0019 reaches accepted."
  - date: "2026-08-20"
    event: "Schemas convention migration: config gains core_schema: true — schemas/ is now formally the core-schema delta (target.cue + examples.cue + spec.md required from accepted); cue.mod refreshed to enhancements.opmodel.dev/0015/schemas, CUE v0.17.0. Structure/metadata only."
  - date: "2026-08-20"
    event: "Schemas convention follow-up: examples.cue authored (backup/k8up/velero cast, 20 assertion pins) and schemas/spec.md drafted (ten sections). Fixed a latent self-reference bug in target.cue stamping fields (catalogVersion: catalogVersion resolves to itself and stamps nothing) with label aliases, the device core catalog.cue already uses."
  - date: "2026-08-20"
    event: "Re-baselined on 0019's acceptance, discharging the trigger the 2026-08-19 event set. The library/operator integration surface is rewritten against the single-build render pipeline: the contract inventory becomes a pure CUE fold over the platform's embedded catalogs (0019 D5) instead of a materialize computation; D2's no-matcher-change invariant is restated against the in-build match glue (0019 D10 reproduces the kernel's exact pair set; match.go and #matchers are deleted by 0019 D10/D17); D3's acceptance flow now feeds operator-side platform-package regeneration (0019 D6) with no held platform to invalidate (0019 D8); the D37 arity guard and default-class fill move to platform assembly. OQ6 closed as answered — the store key it asked about was deleted with the store — and its blast-radius residue plus 0019's deferred OQ9 are filed as OQ8 (what regenerates the platform package, and what keys it). OQ1-OQ4 annotated with their post-0019 shapes. Pre-0019 measurements stay in place, dated, as the record of the code the design was argued against. D1/D2/D3 carry Revised lines; the decisions themselves are unchanged."
  - date: "2026-08-21"
    event: "Authoring surface designed and decided (D9-D12), from a walk grounded in core/src/{module,component,resource,trait,transformer,catalog}.cue read the same day. D9: registration is authored as a transformer-registration #Resource contract in catalog_opm rendered by a catalog_opm transformer — the RBAC gate requires the CR to be rendered output applied under tenant impersonation, and everything rendered flows component→transformer, so #Module gains no authored field (a derived #provides fold is deferred until a consumer exists); #Trait rejected on appliesTo and on providerRef being instance-level. D10: the claim names a published catalog artifact only, refused structurally (acceptance imports the root package and requires a #Catalog value), transformer code never rides the CR, and single-repo providers publish catalog+module in lockstep — the module+package+version generalization (module-hosted transformers) is preserved additively in alternatives. D11: every claim field is derived (catalog/version/provides from the provider catalog's identity package via the module's cue.mod dependency) or stamped (providerRef from instance context), with exact-equality provides verification at acceptance; the only authored fact is the dependency version. D12: instance-derived dot-joined CR name, so duplicates arbitrate at acceptance naming the claimant, never at SSA apply. D3 revised in place to point at the surface. contracts/ scaffolded with the compilable authoring-surface shapes and pinned k8up examples; target.cue and spec.md co-updated. OQ11 filed (ModulePackage readiness must not let the registration gate its own package) and OQ12 (registration multiplicity per module)."
  - date: "2026-08-21"
    event: "OQ walk concluded: D5-D8 and D13-D15 locked. OQ1/OQ10 resolved-by-D5 (duplicate guard, at generation), OQ3 by D6 (opm platform pull), OQ4 by D7 (no predicate-stability rule; widening gate to 0011), OQ7 by D8 (acceptance-time committed-resolution comparison, exact under GA discipline), OQ8 by D13 (edge-triggered level-computed regeneration, identity = generation + active claims, blast radius accepted), OQ11 by D14 (registration excluded by kind from package readiness; pure-registration modules legal), OQ12 by D15 (one registration per module). OQ2 dissolved by D2's class rejection; OQ5 deferred to a future aliasing entry; OQ9 deferred to the guard's implementation slice with a measured candidate definition validated against catalog_opm's 8-transformer bucket. Mid-walk: provider classes rejected (D2 revised in place, 0010 D37 stands, entry retitled); D13 re-grounded on the same-day D9-D12 authoring-surface work. Zero open questions remain."
  - date: "2026-08-21"
    event: "Pre-promotion compaction pass (enhancement-compaction): OQ1-OQ4, OQ7, OQ8, OQ10-OQ12 collapsed to bare resolved-by pointers; OQ6's answered line trimmed; OQ5 and OQ9 keep their deferral annotations (no filed target — the prose is the hand-off). No weaves, no tombstones: the draft was maintained under the in-place rule."
  - date: "2026-08-21"
    event: "Accepted. All twelve Open Questions closed (nine resolved into D2/D5-D8/D13-D15, OQ6 answered, OQ5/OQ9 explicitly deferred with hand-off context); semver set to major per 06-operational's break-in-effect reasoning; plan.yaml authored — nine slices across core, library, catalog, opm-operator and cli with three cross-enhancement dependencies on 0019's registry/build slices, D4 and D7 recorded as deliberately unsliced; Cross-References re-verified (the tenancy reference repointed to the archived openspec design, opm-operator/docs/TENANCY.md no longer exists)."
    semver: major
  - date: "2026-08-21"
    event: "Delivery-plan extraction: plan.yaml/PLAN.md moved out of the entry into the plans/ tree under the one-way reference rule (plans cite enhancements; enhancements never cite plans); in-entry file references reworded to the generic form. Structure/metadata only."
# Peer enhancements this one builds on or interacts with.
related:
  - "0010"
  - "0011"
  - "0019"
supersedes: []
superseded_by: null
