# Enhancement Index

<!-- Generated by `task index` — do not edit by hand. -->

| ID | Area | Status | Semver | Impl | Last event | Title |
| -- | ---- | ------ | ------ | ---- | ---------- | ----- |
| [0001](./0001/) | core | implemented | minor | complete | 2026-07-24 — Implemented — accepted → implemented. Final graduation gate closed: enhancement 0006's CLI kernel-adoption slice (C2, D9) landed 2026-07-20 in cli 1.0.0-alpha.4, completing the last accepted→implemented criterion (the cli/ rewrite onto the library kernel). Verified in cli/ today: go.mod requires github.com/open-platform-model/library v1.0.0-alpha.8; the pkg/render fork is deleted (directory gone); 13 Go files import open-platform-model/library/opm; render (internal/workflow/render/kernel.go NewKernel → kernel.New + WithRegistry + WithSchemaLoader → env.kernel.Compile), platform materialize (internal/platform/materialize.go → MaterializedPlatform), and handoff verify (internal/workflow/handoff/verify.go → k.Compile) all flow through the kernel — full adoption, not a token import. All producer + consumer rewrites now complete: core@v0, library v0.3.0+, catalogs/opm@v0, opm-operator (2026-06-12), cli (via 0006, 2026-07-20). Workspace modules/* mass-rewire stays non-blocking consumer maintenance (D23 wave). implementation.status in-progress → complete; implementation.date 2026-07-20 (CLI slice landing = final piece). README gains the implementation-status quote block and a fourth Deviations entry (CLI rewrite carved into 0006). | #Platform Redesign Umbrella |
| [0002](./0002/) | core | implemented | major | complete | 2026-06-30 — Part B (closing enhancements-wording cleanup) landed and 0002 promoted accepted → implemented. Downstream entries renamed to instance vocabulary: 0008 (target.cue codegen surface — #ModuleInstanceSpec/Status/CRD, instanceUUID, group opmodel.dev, kind/plural/singular/shortName mi, Platform CRD group; prose), 0006 (ModuleRelease→ModuleInstance narrative + target.cue, operator-moduleinstance-owner-marker task), 0007 (ModuleRelease→ModuleInstance and GitOps Release→ModulePackage), 0003 (synth.Release→synth.Instance, release.cue→instance.cue prose). Approved scope-extension also renamed opm release→opm instance command refs across the drafts (bare-noun release left). Metadata graph repaired (related += 0002 on 0003/0007/0008; 0007 += 0006; 0006 already linked); INDEX+GRAPH regenerated; whole-repo task vet green. 0001 left as historical record; 0004/0005/0009/0000 confirmed clean. implementation.status → complete (date 2026-06-30). Still separately tracked (out of 0002 scope): modules/+releases/ re-pin onto core@v1/catalog_opm@v1 with the release.cue→instance.cue sweep, and the opm-operator main-spec hygiene pass. | Rename the Release artifact family to Instance vocabulary (cross-cutting) |
| [0003](./0003/) | cli | superseded | major | partial | 2026-07-29 — Compacted under the new mutability rules. The narrative documents (01, 02, 04, 06 and 05-risks' risk narrative) collapsed to stubs naming what they covered and which successor owns it; the Open Questions block reduced from 42KB to one line per question, each naming the successor that inherited it (0010 OQ1/OQ3/OQ8, 0011 OQ2/OQ3/OQ4/OQ5/OQ6) or recording that none did (OQ3, OQ8). Decision log kept in full with D17/D19/D20/D25 now marked in place as superseded by D19/D21/D24/D27, so it is safe to read linearly. Kept deliberately: 05-risks' per-site Blast Radius audit, measured against real code and reproduced nowhere else, plus all of experiments/ and research/. No decision or OQ number was reused or renumbered. Entry size 187KB -> 60KB. | OPM Module Publishing Workflow |
| [0004](./0004/) | cross-cutting | accepted | none | not-started | 2026-06-18 — Accepted | Automated CUE Dependency Updates via Dagger |
| [0005](./0005/) | cross-cutting | draft | - | not-started | 2026-06-20 — Design direction set from research synthesis — Model B (sugar composes onto generated mirror) chosen; single cross-cutting umbrella; core change ruled out as not required | Kubernetes-Native Refocus: Generated Mirror and Composed Abstractions |
| [0006](./0006/) | cli | implemented | none | complete | 2026-07-20 — Implemented — all nine resolved slices landed (A1/A2/A4/A6/B2/C1/C2/C3/A5; A3 reverted per D31, B1 cancelled). implementation.status -> complete; status accepted -> implemented. README gains the implementation-status block and the Deviations from Design index (A3/D31 revert, D32 noun-first reversal, D36 CUE retarget/relocation, D40 inventory-stable redefinition, C3 SSA full-intent correction + LD4a race, D18 scope widening into C3, OQ18). Deferred open questions at graduation: OQ15 (stale-set relation consistency), OQ16 (operator apply-time collision guard), OQ18 (dev-tag range pollution) — each a candidate follow-on, none in-scope gaps. | CLI CR Inventory, Library Kernel Adoption, and Operator Handoff |
| [0007](./0007/) | cross-cutting | draft | - | not-started | 2026-06-23 — Design direction set from architecture review — apply-layer passthrough on the CLI + operator (kustomize embedded via krusty, output folded into the existing SSA/inventory/prune machinery); core schema and library kernel held untouched to preserve kernel purity. Two scope questions seeded as Open Questions (kustomize-build vs raw-only; relationship to 0005) | Manifest Passthrough: Side-Channel Raw and Kustomize Manifests |
| [0008](./0008/) | cross-cutting | draft | - | not-started | 2026-06-25 — Drafted — seeded from a deep-research sweep on CUE→CRD and CUE→Go tooling (research/findings.md) | CUE-Native CRD Schemas as Single Source of Truth |
| [0009](./0009/) | cross-cutting | draft | - | not-started | 2026-06-29 — Researched CUE attribute longevity (research/cue-attribute-longevity.md) — no removal planned; CUE's custom-functions feature is itself attribute-based (@extern). D5 placement corrected (field attributes only); attribute-dependence risk added | Operational Primitives: Op, Action, Lifecycle, Workflow |
| [0010](./0010/) | core | draft | major | not-started | 2026-07-30 — Two draft->accepted gates closed by measurement: the migration inventory and the core/SPEC.md line identification. The inventory is research/migration-inventory.md, read directly from the workspace registry (localhost:5000) and the live cluster rather than derived from committed source, and summarised in 06-operational. Counts: 12 live ModuleInstances across 10 modules, 1 Platform whose ENTIRE D29/D31 rewrite surface is three `range` lines, 39 published module repositories, 4 catalog repositories, 17 non-module artifacts in the namespace. Four findings change what the migration has to do, and the first is the largest. THE LIVE FLEET DOES NOT RESOLVE AGAINST THE REGISTRY THIS WORKSPACE PUBLISHES TO: opm-operator-controller-manager carries no CUE_REGISTRY env, no registry arg and no mounted config, so CUE resolves through its default central registry, and six of twelve live instances name coordinates ABSENT from localhost:5000 (radarr, sonarr, sabnzbd have no repository there at all; jellyfin, k8up, cert_manager, istio_ambient, seerr are published only at lower versions than the ones deployed). The migration spans two registries; the central one had no egress at measurement time and is recorded as an open collection task that gates SCHEDULING the migration, not running it. This is the one part of the gate satisfied by a stated absence rather than by data, and it is stated rather than papered over. Second: opmodel.dev/catalogs/opm_experimental AND opmodel.dev/catalogs/opm-experimental both exist, both holding v1.3.0-alpha, with the live platform subscribed to the underscore form — under D24 a catalog path is the permanent prefix of every contract FQN, so two spellings are two key spaces and a module built against one cannot match a platform subscribed to the other. That is the failure D1 and D8 exist to prevent, already live. Third: opmodel.dev/modules/opm-platform sits in module space while probably not being a module, defeating 0011 D5's premise that module/catalog/schema space are distinguishable by path alone. Fourth: the source tree and the registry are neither subset nor superset — modules/ holds 12 directories, three deployed-but-unpublished (radarr, sabnzbd, sonarr) and two sourced-but-unpublished-anywhere (cdi, snapshot_controller) — so both lists must reconcile before the fleet republish step. All three defects are recorded as gate items to resolve BEFORE the migration runs, since each is a wrong input to it rather than a step within it. Also measured: the catalogs/opm subscription's `<=1.0.0-alpha.6` upper bound is enhancement 0006's A5 patch still working, since the registry holds v1.0.0-dev.1785176190.g76f3cc6 and SemVer orders alpha before dev; under D31 that exclusion becomes structural and 0006 OQ18 closes as a side effect. SEPARATELY, the SPEC.md pass replaced one section-name bullet with 20 line-ranges across 9 of the file's 15 sections (668 lines, read 2026-07-30), and it surfaced something that is not a mechanical re-type: SPEC.md states THREE TIMES, normatively, that component labels unify upward from every attached primitive (:216 constraint, :222 rationale, :658 the justification for label-based matching) — the exact union claim D26's correction found has no implementing code. The co-update must settle OQ16 or narrow the claim. :657's 'why match is FQN-keyed and always unifies' likewise argues from distinct-SemVers-are-distinct-keys, a premise D24 removes for contracts. | Module and Catalog Identity |
| [0011](./0011/) | cli | draft | minor | not-started | 2026-07-29 — D9 recorded and OQ7 filed, both consequences of enhancement 0010's D24-D28. D9 gives `opm catalog publish` a COMPATIBILITY GATE: for each primitive, pull the last published build shipping that name at that apiVersion and refuse a non-additive change, per 0010 D27. It is not redundant with the consumer-side check, and the measurement says why — 0010's experiments/02 found that the match rung catches a removed field and a narrowed type but NOT a changed default, because two builds disagreeing on a default unify to a non-concrete value, so the match passes and the render fails later on an incomplete value naming a field rather than a build. The one violation the consumer cannot catch is the one that silently moves output. Two bounds are stated rather than assumed: subsumption is transitive so the immediate predecessor secures the whole history by induction, but only while every build passes the gate, and D11 in 0010 records that `cue mod publish` keeps working — whether that gap gets a read-side counterpart is 0010 OQ13. The implementation is deliberately unspecified because `cue.Value.Subsume`'s behaviour across two builds with closedness and defaults in play is UNMEASURED; 0010's experiment already carries the four builds a correct gate must classify, so sequencing that measurement before specifying the command is a graduation gate — a gate that cannot be built as described sends 0010 D27 back to publisher discipline. OQ7 asks who owns a catalog name. The provider-catalog scenario introduces exactly the actor D5's owner-scoping exists for, and D5 gives modules an owner segment while leaving catalogs flat — where the argument applies with MORE force, since a catalog's name is the permanent prefix of every FQN every module matches on and 0010 D24 makes contract keys outlive any single build. A third party publishing a k8up provider catalog today either gets OPM to host `opmodel.dev/catalogs/k8up`, which reads as endorsement and makes OPM the arbiter D5 wanted to avoid, or publishes under their own domain and is unresolvable for anyone who has not edited CUE_REGISTRY, which D5's own rationale rules out. | Module and Catalog Publishing |
| [0012](./0012/) | library | draft | - | not-started | 2026-07-27 — Drafted | Kubernetes as a First-Class Kernel Platform |
| [0013](./0013/) | cross-cutting | draft | - | not-started | 2026-07-27 — Design revised on review: D10 supersedes D1, D11 supersedes D4, D12 amends D9, and OQ1 is closed. D1 had removed the `#Secret` contract type AND the disjunction inside it, when only the routing metadata needed to move to the attribute — leaving the deployer no slot in which to say "this Secret already exists", and forcing the handle-plus-context-map machinery of D4 to compensate. D10 restores `#Secret` narrowed to `#SecretLiteral | #SecretRef`, so the ATTRIBUTE carries routing (author intent, static, in the published module) and the TYPE carries fulfilment (deployer choice, per environment, type-checked by CUE). D11 replaces handle substitution with resolve-in-place: the kernel rewrites every secret to its `#SecretRef` form, so both arms converge before render, a transformer reads two fields from one branch, and the whole handle format, the `#TransformerContext` addition, the `owned` flag and the output scanner are all deleted. Three properties fall out that the first draft lacked: instance files for supplied secrets do not change at all, plaintext in the render is structurally unrepresentable rather than stripped, and a secret interpolated into a rendered string fails at plain `cue vet` at authoring time. Experiment 02 rebuilt as `02-resolve-in-place` and re-run: all five claims held, and its Go content hash agrees with the CUE one in examples.cue. OQ2 opened — whether the arm rewrite survives the real kernel build path — and is now the sole blocker to acceptance. | Attribute-Declared Secret Fields |
| [0014](./0014/) | cli | draft | none | not-started | 2026-07-29 — Drafted. 0006 shipped the handoff that moves an instance's manager to the operator; nothing moves its definition out of the cluster, so a CLI-first user who now wants GitOps hand-assembles four YAML documents per instance from a `kubectl get -o yaml` dump. Three decisions came from the user on the day: the exported unit is the CR plus its apply envelope rather than a bare CR (D1), export refuses to write unless the published module reproduces `status.lastAppliedRenderDigest` (D2, reusing handoff's gate chain and `VerificationDigest`), and `spec.values` is written verbatim with a warning rather than redacted or refused, because OPM cannot yet identify which values are secret (D3). D4 records the live CR as the sole input. Two findings from reading the code shaped the design. `ApplySpec` (cli/internal/inventory/store.go:119) writes only `spec.module`, `spec.owner`, and `spec.values`, so every CLI-written CR lacks `spec.serviceAccountName` and `spec.prune` — meaning a dump-and-strip export applies under the controller's own identity and orphans its workloads on delete, a behaviour difference invisible in the YAML. And the render-bearing / apply-bearing partition resolves the tension that finding creates: the fields the digest gate verifies are copied untouched, while the fields governing apply identity and deletion sit outside the render digest and can be completed without weakening the guarantee, provided every completion is reported. The partition is enforced in schemas/target.cue rather than asserted in prose. Six open questions; OQ4 (field-manager collision when Flux's kustomize-controller applies over fields owned by opm-cli and the operator) is the load-bearing one and is gated on a runnable experiment, because it decides whether the adoption is as quiet as D2's guarantee implies. | Export a Deployed Instance as GitOps Manifests |

## Status legend

- **draft** — initial design, actively being written
- **accepted** — design agreed upon, ready for implementation
- **implemented** — design has been realized in code
- **superseded** — replaced by a newer enhancement (see `superseded_by` in the entry's config.yaml)
