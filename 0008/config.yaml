id: "0008"
slug: cue-native-crd-schemas
title: CUE-Native CRD Schemas as Single Source of Truth
# One line: the capability OPM will have and does not have today, or the
# rework of one it has. The `feature` admission gate (gates.cue) made durable;
# `task archive:list` renders rejected ideas by this field.
summary: >-
  Make the CUE schema in core the single source of truth for CRD-shaped types, generating the Kubernetes CRD YAML and the Go API types the operator and CLI compile against.
status: draft
# Which OPM area is the primary owner. See enhancements/schema.cue #Area.
area: cross-cutting
# Every repo that ships code/schema/content changes driven by this enhancement.
# MUST include `area`. Validated against the workspace directory map in /CLAUDE.md.
affects:
  - cross-cutting
  - core
  - opm-operator
  - cli
core_schema: true
created: "2026-06-25"
updated: "2026-08-23"
authors:
  - emil@jacero.se
# Append-only timeline of milestones.
history:
  - date: "2026-06-25"
    event: "Drafted — seeded from a deep-research sweep on CUE→CRD and CUE→Go tooling (research/findings.md)"
  - date: "2026-08-20"
    event: "Schemas convention migration: config gains core_schema: true — schemas/ is now formally the core-schema delta (target.cue + examples.cue + spec.md required from accepted); cue.mod refreshed to enhancements.opmodel.dev/0008/schemas, CUE v0.17.0. Structure/metadata only."
  - date: "2026-08-20"
    event: "Schemas convention follow-up: examples.cue authored (worked #CRD instances moved from target.cue, 12 assertion pins, recorded must-fail cases) and schemas/spec.md drafted (7 NEW envelope definitions)."
  - date: "2026-08-21"
    event: "Decision Kind gate retrofit: every decision annotated with Kind (contract | policy | scope) per the new template convention. Classification only — no decision content changed."
  - date: "2026-08-22"
    event: "Open Questions register extracted: the ## Open Questions section moved from 03-decisions.md to the new 07-questions.md under the seven-doc convention (single canonical location); in-entry location references reworded. Structure/metadata only."
  - date: "2026-08-23"
    event: 'Lifecycle reshape: implementation axis removed (delivery derives from the plans side), 04-graduation.md deleted, summary added.'
# Peer enhancements this one builds on or interacts with.
related:
  - "0006"
  - "0002"
# Lifecycle relationships.
supersedes: []
# Archived ids this entry re-opens. One-way: the archived entry is frozen
# and gets no back-link. See `task archive:list` for prior art.
revives: []
superseded_by: null
